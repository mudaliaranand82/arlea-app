// Include this for React Native Testing Library
import "@testing-library/jest-native/extend-expect";

// Mocking libraries if needed in the future
// jest.mock('expo-linking', () => ({
//   createURL: jest.fn(),
// }));
